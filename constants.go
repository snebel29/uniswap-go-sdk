package uniswap

// ChainID represents a chain ID.
type ChainID int

// Define some known chain IDs.
const (
	MAINNET ChainID = 1
	ROPSTEN ChainID = 3
	RINKEBY ChainID = 4
	GÖRLI   ChainID = 5
	KOVAN   ChainID = 42
)
